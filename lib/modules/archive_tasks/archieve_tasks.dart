import 'package:flutter/material.dart';
class archive_tasks extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('archive',
    style: TextStyle(
      fontSize: 50,
      fontWeight: FontWeight.bold,
    ),),

    );
  }
}
